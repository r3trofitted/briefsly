<h1><%= @issue.title %></h1>

<div id="new_slot">
  <%= render "slots/form", slot: @slot %>
</div>

<%# FIXME: params q or not q?%>
<%= turbo_frame_tag "slots", src: slots_path(q: { repository: params[:repository_name], issue_number: params[:id] }) do %>
  Loading...
<% end %>
